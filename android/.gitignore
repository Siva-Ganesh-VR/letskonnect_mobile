gradle-wrapper.jar
/.gradle
/captures/
/gradlew
/gradlew.bat
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Gradle/AGP build output. The root .gitignore only has "/build/", which is
# anchored to the repo root and does not cover android/build or app/build.
build/
.kotlin/
.artifacts/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
